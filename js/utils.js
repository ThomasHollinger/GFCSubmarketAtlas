function num(v,d=0){ if(v===null||v===undefined||v==='')return '—'; const n=Number(v); if(Number.isNaN(n))return v; return n.toLocaleString(undefined,{maximumFractionDigits:d,minimumFractionDigits:d}); }
function normName(s){ return String(s||'').toLowerCase().replace(/&/g,'and').replace(/\b(school|elementary|middle|high|senior|junior|jr|sr|the|of|academy)\b/g,'').replace(/[^a-z0-9]+/g,' ').trim(); }
function downloadText(filename,text){ const blob=new Blob([text],{type:'text/csv'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=filename; a.click(); URL.revokeObjectURL(a.href); }
function pointInRing(pt, ring){ const x=pt[0], y=pt[1]; let inside=false; for(let i=0,j=ring.length-1;i<ring.length;j=i++){ const xi=ring[i][0], yi=ring[i][1], xj=ring[j][0], yj=ring[j][1]; const intersect=((yi>y)!=(yj>y))&&(x<(xj-xi)*(y-yi)/(yj-yi)+xi); if(intersect)inside=!inside;} return inside; }
function pointInGeometry(pt, geom){ if(!geom)return false; if(geom.type==='Polygon') return pointInRing(pt, geom.coordinates[0]); if(geom.type==='MultiPolygon') return geom.coordinates.some(poly=>pointInRing(pt, poly[0])); return false; }
function gradeFor(score){ if(score==null) return 'Pending'; if(score>=9) return 'A'; if(score>=8) return 'B'; if(score>=7) return 'C'; if(score>=6) return 'D'; return 'F'; }
function fmtScore(v){ return v==null ? 'Pending' : Number(v).toFixed(1); }
function csvParse(text){ const rows=[]; let row=[], cur='', inq=false; for(let i=0;i<text.length;i++){ const c=text[i], n=text[i+1]; if(c==='"'){ if(inq&&n==='"'){cur+='"';i++;} else inq=!inq; } else if(c===','&&!inq){ row.push(cur); cur=''; } else if((c==='\n'||c==='\r')&&!inq){ if(c==='\r'&&n==='\n')i++; row.push(cur); if(row.some(x=>x!=='')) rows.push(row); row=[]; cur=''; } else cur+=c; } row.push(cur); if(row.some(x=>x!=='')) rows.push(row); const head=rows.shift()||[]; return rows.map(r=>Object.fromEntries(head.map((h,i)=>[h.trim(),(r[i]||'').trim()]))); }
