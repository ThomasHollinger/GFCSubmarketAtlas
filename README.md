# Gulf Coast Submarket Atlas v2.9.92

New Deal Market Preview buttons now use smaller typography and tighter horizontal padding so the `3 Mile Preview` and `5 Mile Preview` labels fit cleanly inside their buttons.

No other New Deals behavior, Firebase authentication, map behavior, or data was changed.

Upload the included `index.html` over the existing `index.html` in GitHub. Keep the existing `js/app-v2_9_91.js` and other files unchanged.
